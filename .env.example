cd server
cp .env.example .env
```

Then open `.env` in VS Code and fill in:
```
DB_HOST=localhost
DB_USER=postgres
DB_PASS=        ← leave blank if you didn't set a password
DB_NAME=influscout
DB_PORT=5432
DB_SSL=false

ADMIN_SECRET=anyRandomStringYouWant